# Falcon Design

Bilingual (Arabic / English) corporate website for Falcon Design.

Edit content in `src/lib/data/content.ts`. Assets live in `public/`.

```bash
npm install
cp .env.example .env.local
npm run dev
```

Configure SMTP or Resend in `.env.local` for contact form delivery.
